import { MyShopDirective } from './my-shop.directive';

describe('MyShopDirective', () => {
  it('should create an instance', () => {
    const directive = new MyShopDirective();
    expect(directive).toBeTruthy();
  });
});
